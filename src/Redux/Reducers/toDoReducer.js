let initialState = {
  toDo: ["learn React", "learn Redux", "learn Javascript"],
  errors: [],
};
const toDoReducer = (state = initialState, action) => {
  return state;
};

export default toDoReducer;
